/**
 * _abs - C function that computes the absolute value of an number
 * @n: Number that will be checked
 * Return: 0
 */
int _abs(int n)
{
if (n < 0)
{
n = (-1) * n;
return (n);
}
else
return (n);
}

#include <stdio.h>
/**
 * main - C program that prints the lowercase alphabet
 * in reverse, followed by a new line.
 * followed by a new line.
 * Return: 0
 */
int main(void)
{
int tebahpla = 'z';
while (tebahpla >= 'a')
putchar(tebahpla--);
putchar('\n');
return (0);
}
